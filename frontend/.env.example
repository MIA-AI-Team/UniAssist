BACKEND_INTERNAL_URL=http://localhost:8001
APP_URL=http://localhost:3000
# Generate a persistent secret of at least 32 characters; never use the backend JWT secret.
SESSION_SECRET=
BACKEND_TIMEOUT_MS=180000

