services:
  ai-service:
    build: .
    container_name: ai-tutor-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      MOCK_MODE: ${MOCK_MODE:-true}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
