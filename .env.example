# Root compose overrides (optional). Service-specific secrets live in:
#   ai-service/.env
#   backend/.env

POSTGRES_USER=uniassist
POSTGRES_PASSWORD=uniassist
POSTGRES_DB=YourPassowrd
POSTGRES_PORT=5432

AI_SERVICE_PORT=8000
BACKEND_PORT=8001

MOCK_MODE=true
